#!/usr/bin/env ruby
require 'webrick'

server = WEBrick::HTTPServer.new(
  :Port => 3000,
  :BindAddress => '0.0.0.0',
  :DocumentRoot => File.expand_path(Dir.pwd)
)

trap('INT') do
  server.shutdown
end
server.start
